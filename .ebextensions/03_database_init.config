container_commands:
  01_init_database:
    # Only initialize database if it doesn't already exist
    # This preserves the deployed database file with existing data
    command: "test -f campus_resource_hub.db || python3 init_db.py campus_resource_hub.db"
    leader_only: true
